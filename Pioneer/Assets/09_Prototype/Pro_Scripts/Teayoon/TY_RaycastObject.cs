using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TY_RaycastObject : MonoBehaviour, IBegin
{
    public string info = "";

    // Start is called before the first frame update
    void Init()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
